Huiji Local VisualEditor bridge extension 1.0.1

This Edge extension is only the browser-side bridge. It requires the local
Parsoid and API proxy included in the complete HuijiLocalVisualEditor package.

For normal use, do not install this ZIP manually. Extract the complete package
and double-click the Chinese-named launcher CMD; it loads this extension into a
separate Edge profile automatically.
